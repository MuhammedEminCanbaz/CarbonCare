fastapi==0.112.2               # Web API geliştirmek için hafif, hızlı framework
uvicorn==0.30.6                # FastAPI uygulamasını çalıştırmak için ASGI server

SQLAlchemy==2.0.32             # Veritabanı işlemleri için güçlü ORM
alembic==1.13.2                # SQLAlchemy ile veritabanı şeması değişikliklerini yönetmek (migrasyonlar)
aiosqlite==0.20.0              # Asenkron SQLite bağlantısı için (FastAPI async çalıştırırken gerekebilir)

python-jose==3.3.0             # JWT token işlemleri (kimlik doğrulama, oturum yönetimi)
bcrypt==4.0.1                  # Kullanıcı şifrelerini güvenli şekilde hash’lemek
passlib==1.7.4                 # Şifre hash’leme ve doğrulama için ek araçlar
cryptography==43.0.0           # Şifreleme ve güvenlik işlemleri için temel kütüphane

httpx==0.27.2                  # Asenkron HTTP istekleri göndermek (örneğin, Gemini API’ye çağrı yapmak)
pytest==8.3.2                  # Projeyi test etmek için temel test framework’ü
pytest-asyncio==0.24.0         # Asenkron kodların pytest ile test edilmesini sağlamak

jinja2==3.1.4                  # HTML şablonlama motoru (dinamik HTML sayfaları üretmek için)
python-multipart==0.0.9        # Form verisi veya dosya yükleme işlemleri için
aiofiles==24.1.0               # Asenkron dosya okuma/yazma işlemleri için

python-dotenv==1.0.1           # .env dosyasından ortam değişkenlerini yüklemek
markdown==3.7                  # Markdown metinleri HTML’ye dönüştürmek için
beautifulsoup4==4.12.3         # HTML verisi işlemek, parse etmek için (web scraping veya HTML düzenleme)

google-generativeai==0.7.2     # Gemini API’yi kullanmak için Google’ın sağladığı kütüphane
langchain==0.2.15              # LLM iş akışlarını yönetmek, zincirleme yapılar kurmak için
langchain-google-genai==1.0.10 # Langchain ile Gemini entegrasyonu için özel modül
